import { Inter, Space_Grotesk as SpaceGrotesk, Fira_Code as FiraCode } from 'next/font/google'

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-inter',
})

export const spaceGrotesk = SpaceGrotesk({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-space-grotesk',
})

export const firaCode = FiraCode({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-fira-code',
})
